# Pinned deliberately. CI resolves this file fresh on every push to main and
# publishes the result to ghcr.io/<repo>:latest, so an unpinned file means a
# poisoned or merely incompatible upstream release ships with no diff for
# anyone to review -- and CLAUDE.md makes load-bearing claims about specific
# versions ("the pinned gunicorn version (26.x) parses its app argument as a
# Python expression", and control_socket_disable existing at all), which were
# not actually pinned. gunicorn has already changed relevant behaviour once at
# 25.1.
#
# These are exact versions, not a lockfile: transitive dependencies are still
# resolved freely and unhashed. A hash-pinned lockfile is the stronger fix and
# is not done here.
#
# PyYAML was dropped at this pin: the YAML registry it served was deleted at
# build step 7 and nothing under nethub/, tests/ or scripts/ imports it. The
# `yamllint` used by CI is a separate tool installed by the workflow, not a
# runtime dependency of this package.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.3.0
# Schema migrations (PLAN.md WS-6; nethub/schema.py, nethub/migrations/).
# alembic is pinned in its own right, not left to Flask-Migrate's range: it is
# what actually rewrites the database at every upgrade.
Flask-Migrate==4.1.0
alembic==1.20.0
gunicorn==26.2.0
# Seals device credentials into the job row (PLAN.md WS-7). Already installed
# as a paramiko dependency; pinned in its own right because NetHub now calls
# it directly and a change in it changes what can open a stored credential.
PyNaCl==1.6.2
pytest==9.1.1
netmiko==4.7.0
ntc-templates==9.2.0
