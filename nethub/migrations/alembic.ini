# Alembic settings for `flask --app nethub db ...`. Deliberately minimal:
# there is no [loggers] section, because env.py must not reconfigure logging
# -- it also runs inside gunicorn at startup (nethub.schema), where
# logging.config.fileConfig() would replace gunicorn's handlers and disable
# every logger that already exists.
[alembic]
